# 🌞 คู่มือการใช้งาน PV Load Profile Generator

> **เวอร์ชัน:** 1.0 | **อัปเดต:** พฤษภาคม 2569

---

## 📌 โปรแกรมนี้ทำอะไร?

**PV Load Profile Generator** เป็นเครื่องมือวิเคราะห์พลังงานสำหรับอาคาร/โรงงาน  
ป้อนข้อมูลการใช้ไฟฟ้าและเป้าหมายรายเดือน → โปรแกรมจะสร้าง:

| ผลลัพธ์ | รายละเอียด |
|---|---|
| 📊 **Load Profile 8,760 ชั่วโมง** | ข้อมูลการใช้ไฟฟ้ารายชั่วโมงตลอดทั้งปี |
| ☀️ **PV Generation** | การผลิตไฟฟ้าจากโซลาร์เซลล์ (จำลองด้วย pvlib + ข้อมูลสภาพอากาศจริง) |
| ⚡ **Net Load** | ไฟที่ต้องซื้อจากการไฟฟ้า = Load − PV |
| 📥 **ไฟล์ CSV** | ผลลัพธ์พร้อมใช้กับ PVSyst, SAM, Homer |

---

## 🖥️ วิธีเข้าใช้งาน

### ✅ แบบที่ 1 — ผ่าน Link (ถ้าเจ้าของ Deploy บน Cloud แล้ว)
เพียงกด Link ที่ได้รับ เปิด Browser แล้วใช้งานได้เลย ไม่ต้องติดตั้งอะไรทั้งสิ้น

### 💻 แบบที่ 2 — รันบนเครื่องตัวเอง
ต้องมี Python ติดตั้งไว้ก่อน จากนั้น:

```bash
# 1. Clone หรือ Download โปรเจค
git clone https://github.com/[your-username]/PV_loadprofie.git
cd PV_loadprofie

# 2. สร้าง Virtual Environment
python -m venv venv

# 3. เปิดใช้งาน (Windows)
venv\Scripts\activate

# 4. ติดตั้ง Package
pip install -r requirements.txt

# 5. รันโปรแกรม
streamlit run app.py
```

จากนั้นเปิด Browser ไปที่ `http://localhost:8501`

---

## 📋 ขั้นตอนการใช้งาน


### ขั้นตอนที่ 1 — เตรียมไฟล์ `Inputs.xlsx`

> 💡 **ไม่มีไฟล์?** กดปุ่ม **"📄 ดาวน์โหลด Template Inputs.xlsx"** ที่ Sidebar ได้เลย  
> ไฟล์ตัวอย่างมีข้อมูลครบทุก Sheet พร้อมแก้ไขค่าตามจริง


ไฟล์นี้มี **3 Sheet** ต้องกรอกให้ครบ:

#### Sheet 1: `Weekday` — รูปแบบการใช้ไฟวันทำงาน (จันทร์–ศุกร์)

| Time | Load_kW |
|---|---|
| 00:00 | 20 |
| 06:00 | 80 |
| 12:00 | 100 |
| 18:00 | 60 |
| 23:59 | 25 |

> 💡 **ไม่จำเป็นต้องกรอกทุกชั่วโมง** — โปรแกรมจะ Interpolate เพิ่มให้อัตโนมัติ  
> แต่ควรมีจุดยึดที่ **00:00** และ **23:59** เสมอ

#### Sheet 2: `Weekend` — รูปแบบการใช้ไฟวันหยุด (เสาร์–อาทิตย์)

| Time | Load_kW |
|---|---|
| 00:00 | 15 |
| 08:00 | 50 |
| 20:00 | 45 |
| 23:59 | 20 |

#### Sheet 3: `Targets` — เป้าหมายการใช้ไฟรายเดือน (kWh)

| Month | Target_kWh |
|---|---|
| 1 | 45000 |
| 2 | 42000 |
| 3 | 48000 |
| ... | ... |
| 12 | 47000 |

> ⚠️ **ต้องมีครบทั้ง 12 เดือน** (Month = 1 ถึง 12)  
> ค่า Target_kWh ดูได้จากบิลค่าไฟรายเดือน

---

### ขั้นตอนที่ 2 — อัปโหลดไฟล์

1. ดูที่ **Sidebar ด้านซ้าย**
2. กดปุ่ม **"Browse files"** ใต้หัวข้อ "📁 ข้อมูล Input"
3. เลือกไฟล์ `Inputs.xlsx` ที่เตรียมไว้
4. โปรแกรมจะโหลดข้อมูลและแสดงกราฟ Daily Profile ทันที

---

### ขั้นตอนที่ 3 — ตั้งค่าระบบ PV (ถ้าต้องการ)

ที่ Sidebar ส่วน **"⚙️ ตั้งค่าระบบ PV"** ปรับค่าตามระบบที่ต้องการวิเคราะห์:

| พารามิเตอร์ | ค่าเริ่มต้น | คำอธิบาย |
|---|---|---|
| **Latitude** | 13.75 | ละติจูดของสถานที่ (กรุงเทพฯ) |
| **Longitude** | 100.51 | ลองจิจูดของสถานที่ |
| **มุมเอียงแผง (°)** | 15° | มุมเอียงของแผงโซลาร์ (เหมาะกับไทย: 10–15°) |
| **Azimuth (°)** | 180° | ทิศที่หันหน้า (180 = ทิศใต้ ดีที่สุดในไทย) |
| **ขนาด DC (kW)** | 120 kW | กำลังการผลิตรวมของแผง |
| **ขนาด AC (kW)** | 100 kW | ขนาด Inverter |
| **Inverter Efficiency** | 96% | ประสิทธิภาพ Inverter |

> 💡 **หาค่า Latitude/Longitude ได้จาก Google Maps:**  
> คลิกขวาที่สถานที่ → คัดลอกตัวเลขที่ขึ้นมา

---

### ขั้นตอนที่ 4 — รัน PV Simulation

1. กดปุ่ม **"▶ Run Simulation"** สีม่วงที่ Sidebar
2. โปรแกรมจะดึงข้อมูลสภาพอากาศ TMY (Typical Meteorological Year) จากฐานข้อมูล **PVGIS** ของ EU
3. รอประมาณ **15–30 วินาที** (ต้องการ Internet)
4. กราฟและตัวเลขสรุปจะปรากฏที่ Tab 2

> ⚠️ **ต้องการ Internet** สำหรับขั้นตอนนี้เพื่อดึงข้อมูลสภาพอากาศ

---

## 📊 การอ่านผลลัพธ์

### Tab 1: Daily Profile
- **เส้นสีน้ำเงิน** = รูปแบบการใช้ไฟวันธรรมดา
- **เส้นสีแดง** = รูปแบบการใช้ไฟวันหยุด
- ตรวจสอบว่ารูปทรงสมเหตุสมผลก่อนไปขั้นถัดไป

### Tab 2: Yearly + PV Simulation

**กราฟรายเดือน (Bar Chart)**
- แท่งสีม่วง = การใช้ไฟที่คำนวณได้
- เส้นประสีเหลือง = เป้าหมาย (Target) ที่กรอกไว้
- ถ้าทั้งสองค่าใกล้กัน = ✅ การปรับสัดส่วนถูกต้อง

**กราฟสัปดาห์แรก (Load vs PV vs Net Load)**
- **เส้นสีม่วง** = การใช้ไฟของอาคาร (Building Load)
- **พื้นที่สีเหลือง** = ไฟที่โซลาร์ผลิตได้ (PV Generation)
- **เส้นประสีเทา** = ไฟที่ต้องซื้อจากกริด (Net Load)
- **เส้นแดงประ** = เส้น 0 kW (ถ้า Net Load ต่ำกว่านี้ = มีไฟ Export กลับกริด)

**ตัวเลขสรุป (3 Metrics)**

| Metric | ความหมาย |
|---|---|
| **Total Load (kWh/ปี)** | พลังงานที่ใช้ทั้งปี |
| **PV Generation (kWh/ปี)** | พลังงานที่โซลาร์ผลิตได้ทั้งปี |
| **PV Coverage Ratio (%)** | สัดส่วนที่โซลาร์ครอบคลุมความต้องการ |

---

## 💰 การวิเคราะห์เศรษฐศาสตร์ (Tab 4)

> ⚠️ ต้องรัน **PV Simulation** ก่อน (Tab 2) จึงจะใช้งาน Tab นี้ได้

### ข้อมูลที่ต้องกรอกเพิ่มเติม

| พารามิเตอร์ | ค่าเริ่มต้น | คำอธิบาย |
|---|---|---|
| **มูลค่าลงทุนรวม (บาท)** | 3,000,000 | ราคาค่าติดตั้งระบบ PV รวมอุปกรณ์ทั้งหมด |
| **ค่าไฟฟ้าที่ประหยัดได้ (บาท/kWh)** | 4.50 | อัตราค่าไฟฟ้าปัจจุบัน (ดูจากบิล) |
| **อัตรา Feed-in / Net Metering (บาท/kWh)** | 2.20 | ราคาที่ได้รับสำหรับไฟ Export กลับกริด (0 = ไม่มี FiT) |
| **อัตราค่าไฟขึ้นราคาต่อปี (%)** | 3.0% | คาดการณ์ว่าค่าไฟจะแพงขึ้นเท่าไรต่อปี |
| **อายุโครงการ (ปี)** | 25 | อายุการใช้งานระบบโซลาร์ |
| **Discount Rate / WACC (%)** | 6.0% | อัตราผลตอบแทนขั้นต่ำที่ยอมรับได้ |
| **อัตราเสื่อมสภาพแผง (%)** | 0.5%/ปี | ประสิทธิภาพแผงลดลงต่อปี |
| **ค่าบำรุงรักษารายปี (% ของ CAPEX)** | 1.0% | ค่า O&M รายปี |

### ผลลัพธ์ที่ได้

| ตัวชี้วัด | ความหมาย | เกณฑ์ดี |
|---|---|---|
| **Simple Payback** | จำนวนปีที่คืนทุน | < 10 ปี |
| **NPV** | มูลค่าปัจจุบันสุทธิ | > 0 บาท |
| **IRR** | อัตราผลตอบแทนภายใน | > Discount Rate |
| **LCOE** | ต้นทุนผลิตไฟฟ้าต่อ kWh | < ค่าไฟปัจจุบัน |
| **Self-Consumption Ratio** | % ของ PV ที่ใช้เองในอาคาร | > 70% |
| **Self-Sufficiency Ratio** | % ความต้องการไฟที่ PV ครอบคลุม | ยิ่งสูงยิ่งดี |

---

## 📥 ดาวน์โหลดผลลัพธ์ (Tab 3)

| ไฟล์ | เนื้อหา | ใช้กับ |
|---|---|---|
| `load_profile_8760.csv` | ค่า Load รายชั่วโมง 8,760 แถว (ไม่มี Header) | PVSyst, SAM, Homer |
| `combined_profile_8760.csv` | Load + PV + Net Load พร้อม Header และ Timestamp | Excel, Python วิเคราะห์ต่อ |

กดปุ่ม **"⬇️ Download"** แล้วไฟล์จะถูกบันทึกลงในโฟลเดอร์ Downloads ของเครื่อง

---

## ❓ แก้ปัญหาเบื้องต้น

| ปัญหา | สาเหตุ | วิธีแก้ |
|---|---|---|
| อัปโหลดไฟล์แล้วขึ้น Error | ชื่อ Sheet ผิด | ตรวจสอบว่า Sheet ชื่อ `Weekday`, `Weekend`, `Targets` (ตรงตัว) |
| กราฟ Daily Profile ดูแปลก | ข้อมูลไม่มีจุด 00:00 | เพิ่มแถว Time=00:00 ใน Weekday และ Weekend |
| PV Simulation ขึ้น Error | ไม่มี Internet | ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต |
| ผลรวมรายเดือนไม่ตรง Target | ปัญหา Targets Sheet | ตรวจสอบว่ามี Month 1–12 ครบ และ Target_kWh ไม่มีค่า 0 |
| ปุ่ม Download combined ไม่ปรากฏ | ยังไม่ได้รัน PV Simulation | กด Run Simulation ก่อน |

---

## 📬 ติดต่อ

หากพบปัญหาหรือต้องการปรับแต่งเพิ่มเติม กรุณาติดต่อเจ้าของโปรเจค

*สร้างด้วย Python · Streamlit · pvlib · Plotly*
